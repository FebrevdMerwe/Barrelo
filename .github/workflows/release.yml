name: Release

on:
  push:
    tags:
      - "v*.*.*"

permissions:
  contents: write

jobs:
  package:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Derive version from tag
        shell: pwsh
        run: |
          $version = "${{ github.ref_name }}".TrimStart("v")
          echo "VERSION=$version" >> $env:GITHUB_ENV

      - run: dotnet test Barrelo.slnx -c Release

      - name: Publish Barrelo.Api
        run: >
          dotnet publish src/Barrelo.Api/Barrelo.Api.csproj
          -c Release -r win-x64 --self-contained true
          -p:Version=${{ env.VERSION }}
          -o ./stage/Barrelo

      - name: Publish BoardSimulator
        run: >
          dotnet publish tools/Barrelo.BoardSimulator/Barrelo.BoardSimulator.csproj
          -c Release -r win-x64 --self-contained true
          -p:Version=${{ env.VERSION }}
          -o ./stage/Barrelo/tools/BoardSimulator

      - name: Add README and LICENSE
        shell: pwsh
        run: |
          Copy-Item README.md ./stage/Barrelo/README.md
          Copy-Item LICENSE ./stage/Barrelo/LICENSE

      - name: Create zip
        shell: pwsh
        run: |
          Compress-Archive -Path ./stage/Barrelo/* -DestinationPath ./Barrelo-${{ env.VERSION }}-win-x64.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: ./Barrelo-${{ env.VERSION }}-win-x64.zip
